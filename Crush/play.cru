var mut evenNumberList = []
var mut radixNumberList = []

for (i, v) <- 0..100 ~>
  if v % 2 == 0 ~>
    println 'Find even number = \(v) index = \(i)'
    evenNumberList.append(v)
  end
  
  else ->
    radixNumberList.append(v)
end

for i <- 0..100 -> if i % 2 == 0 -> println i
